name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '*.md'
      - '*.yml'
  pull_request:
    paths-ignore:
      - '.github/*'
      - '*.md'
      - '*.yml'

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: test
        run: '[System.Environment]::OSVersion.Version'
