name: Test

on:
  push:
    paths:
      - .github/workflows/main.yml
  pull_request:
    paths:
      - .github/workflows/main.yml

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: test
        run: '[System.Environment]::OSVersion.Version'
