name: Test

on:
  push:
    paths-ignore: 
      - README.md
  pull_request:
    paths-ignore: 
      - README.md

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.11, macos-10.12, macos-10.13, macos-10.14, macos-10.15]
    steps:
      - name: test
        run: ls /Applications/
