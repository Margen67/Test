name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'
  workflow_dispatch:
  issues:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      labels: "${{ github.event.issue.labels }}"
    steps:
      - name: test
        run: echo "$labels"
