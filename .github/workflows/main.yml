name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'

#defaults:
#  run:
#    shell: bash
    

jobs:
  test:
    #name: ${{ matrix.os }}
    runs-on: windows-latest #${{ matrix.os }}
    #strategy:
    #  fail-fast: false
    #  matrix:
    #    os: [windows-2016, windows-latest, ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest, macos-11.0]
    #env:
    #  POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: Test 1
        shell: C:\sys64\usr\bin\bash.exe -eo pipefail {0}
        run: Get-Command C:\msys64\usr\bin\bash.exe
