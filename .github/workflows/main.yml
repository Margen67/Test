name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'

#defaults:
#  run:
#    shell: bash
    

jobs:
  test:
    #name: ${{ matrix.runs-on }}
    runs-on: ubuntu-latest #${{ matrix.runs-on }}
    #strategy:
    #  fail-fast: false
    #  matrix:
    #    runs-on: [ubuntu-latest, ubuntu-18.04] #[windows-2016, windows-latest, ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest, macos-11.0]
    #env:
    #  POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: test
        continue-on-error: true
        run: |
          declare -a suffixes=(bionic{,-updates}\ {main,universe})
          for suffix in ${suffixes[@]}; do
            sudo add-apt-repository "deb http://azure.archive.ubuntu.com/ubuntu/ $suffix"
          done
          sudo apt-get update
          for suffix in ${suffixes[@]}; do
            echo $suffix
          done

