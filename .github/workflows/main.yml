name: Test

on:
  push:
    paths-ignore: 
      - README.md
  pull_request:
    paths-ignore: 
      - README.md

jobs:
  test:
    runs-on: windows-latest
    steps:
    - shell: bash
      working-directory: C:\
      run: |
        find . -name "devenv*"
