name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'
  workflow_dispatch:

#defaults:
#  run:
#    shell: bash

jobs:
  test:
    #name: ${{ matrix.runs-on }}
    runs-on: ubuntu-latest #${{ matrix.runs-on }}
    #container: ${{ matrix.container }}
    #strategy:
    #  fail-fast: false
    #  matrix:
    #    container: [ubuntu:devel, '']
    #    runs-on: [ubuntu-latest, windows-latest, macos-latest] #[windows-2016, windows-latest, ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest]
    #env:
    #  POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      #- uses: actions/checkout@v2
      - name: test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          sourceLabelsJson64=$(curl -s -H "Accept: application/vnd.github.symmetra-preview+json" -H "Authorization: Bearer $GITHUB_TOKEN" https://api.github.com/repos/xenia-project/game-compatibility/labels?per_page=5 | jq '[ .[] | '.name' ]' | jq -r '.[] | @base64' )
          for sourceLabelJson64 in $sourceLabelsJson64; do
              sourceLabelJson=$(echo ${sourceLabelJson64} | base64 --decode | jq -r '.')
              echo $sourceLabelJson
          done
