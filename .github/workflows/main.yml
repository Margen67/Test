name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'

#defaults:
#  run:
#    shell: bash
    

jobs:
  test:
    #name: ${{ matrix.runs-on }}
    runs-on: ubuntu-18.04 #${{ matrix.runs-on }}
    #strategy:
    #  fail-fast: false
    #  matrix:
    #    runs-on: [ubuntu-latest, ubuntu-18.04] #[windows-2016, windows-latest, ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest, macos-11.0]
    #env:
    #  POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: test
        run: |
          export distro=$(lsb_release -c)
          wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key|sudo apt-key add -
          sudo add-apt-repository 'deb http://apt.llvm.org/${distro}/ llvm-toolchain-${distro}-12 main'
          supd apt-get update
