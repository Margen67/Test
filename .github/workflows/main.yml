name: Test

on:
  push:
    paths-ignore: 
      - README.md
  pull_request:
    paths-ignore: 
      - README.md

jobs:
  release:
    if: |
      github.event.action != 'pull_request' &&
      github.ref == 'refs/heads/develop'
                 || 'refs/heads/master' &&
      github.repository == 'Margen67/Test'
    env:
      artifact-1: CxbxReloaded-Release-VS2019
      artifact-2: CxbxReloaded-Release-VS2017
    runs-on: windows-latest
    steps:
      - name: echo
        run: echo echo
