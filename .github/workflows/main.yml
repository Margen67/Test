name: Test

on:
  push:
    paths-ignore: 
      - README.md
  pull_request:
    paths-ignore: 
      - README.md

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: test
        run: '& "$env:PROGRAMFILES(X86)\Microsoft Visual Studio\2019\Enterprise\Common7\IDE\devenv.com"'
