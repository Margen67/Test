name: Test

on:
  push:
    paths-ignore: 
      - README.md
  pull_request:
    paths-ignore: 
      - README.md

jobs:
  build:
    runs-on: ARM32
    steps:
    - name: Test
      run: |
        echo ${{ runner.os }}
        dir & ls & pwd
