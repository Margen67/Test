name: Test

on:
  push:
    paths-ignore:
      - '.github/*'
      - '.github/*_TEMPLATE/**'
      - '*.yml'

#defaults:
#  run:
#    shell: bash
    

jobs:
  test:
    #name: ${{ matrix.runs-on }}
    runs-on: ubuntu-latest #${{ matrix.runs-on }}
    #strategy:
    #  fail-fast: false
    #  matrix:
    #    runs-on: [ubuntu-latest, ubuntu-18.04] #[windows-2016, windows-latest, ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest, macos-11.0]
    #env:
    #  POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: test
        run: |
          #sudo add-apt-repository 'deb http://azure.archive.ubuntu.com/ubuntu/ bionic main restricted'
          sudo add-apt-repository 'deb http://azure.archive.ubuntu.com/ubuntu/ bionic main'
          #sudo add-apt-repository 'deb http://azure.archive.ubuntu.com/ubuntu/ bionic universe'
          #sudo add-apt-repository 'deb http://azure.archive.ubuntu.com/ubuntu/ bionic multiverse'
          sudo apt update
          sudo apt-get install -y gcc-6-multilib g++-6-multilib
