version: '{build}-{branch}'

only_commits:
  files:
    - .appveyor.yml

image: macos-mojave #Visual Studio 2019

clone_depth: 1

environment:
  POWERSHELL_TELEMETRY_OPTOUT: 1

build_script:
  - echo $0

test: off
